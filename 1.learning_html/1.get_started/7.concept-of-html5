html5是超文本标记语言，是用标签加尖括号的形式书写各个标签。

1.标签有单、有双。
<img>   <html></html>

2.html5最顶端应该书写
<!DOCTYPE html>  大写的DOCTYPE意为文档类型指定为html。
它并不是没用，它可以避免在不同浏览器中被识别为不同的文档模式。

3.基础骨架
html head body  +<!DOCTYPE html> + title+ meta

--基础骨架平常描述的信息：

5.head，文档的各类属性以及基本信息：
它包括标题，在web中的位置，以及和其它文档的关系。
其内容不会显式地展示给读者。

注：head中一定要有的就是title。

6.title作为浏览器中标签页的标题，必须出现在head内部，用于优化SEO，搜索引擎的最终排序成果。


7.body，图片表格列表等，直接展示给读者的内容。

8.meta用于规定html文档的属性格式，比如编码采用UTF-8等。
<meta charset="UTF-8">

注:meta是一个单标签。
